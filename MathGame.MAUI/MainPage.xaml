<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MathGame.MAUI.MainPage">

    <ContentView>
        <VerticalStackLayout
            Spacing="25"
            Padding="50">
            <Label 
                Text="This is a math game"
                FontSize="32"
                HorizontalOptions="Center"/>
            <Label 
                Text="What game would you like to play?"
                FontSize="32"
                HorizontalOptions="Center"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions >
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text="Addition"/>
                <Button Grid.Column="0" Text="Subtraction"/>
                <Button Grid.Column="0" Text="Multiplication"/>
                <Button Grid.Column="0" Text="Division"/>
            </Grid>
        </VerticalStackLayout>
    </ContentView>

</ContentPage>
